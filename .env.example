# Privacy-friendly, cookieless analytics (PRD §2.2).
# Create a site at https://plausible.io (or self-host Umami and adapt BaseLayout),
# then set this to the site's domain to enable the tracking snippet.
# Leave unset in development — no script is injected when empty.
PUBLIC_PLAUSIBLE_DOMAIN=
